"""Bazel module for the XYG dual-host graph engine.

Cargo remains the source of truth for the Rust crate graph
(`Cargo.toml` / `Cargo.lock`). Root targets wrap `cargo` /
pytest / node so both Python and Node hosts exercise the same
cdylib without requiring crates.io from the Bazel fetch path.
"""

module(
    name = "xyg",
    version = "0.1.0",
)
