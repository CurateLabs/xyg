[project]
name = "xy-fastapi-example"
version = "0"
requires-python = ">=3.11"
dependencies = [
    "xyg",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "numpy>=1.24",
]

[tool.uv]
package = false

[tool.uv.sources]
xyg = { path = "../..", editable = true }
