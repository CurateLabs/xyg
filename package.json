{
  "name": "xyg-dev-tools",
  "private": true,
  "description": "Development-only dependencies for XYG: the Vite/TypeScript client build toolchain and browser test runner",
  "scripts": {
    "build": "node js/build.mjs",
    "build:wasm": "cargo build -p xyg-wasm --release --target wasm32-unknown-unknown && node js/package-wasm.mjs",
    "typecheck": "tsc -p js/tsconfig.json",
    "browser_client_smoke": "node scripts/browser_client_smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "overrides": {
    "postcss": "^8.5.23"
  }
}
